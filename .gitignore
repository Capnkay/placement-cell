# The application server, the private JDK and the database are downloaded by the
# scripts, not authored here. They run to hundreds of megabytes and must never
# be committed.
server/

# Build output, regenerated by build.ps1 on every run.
build/
dist/

# Screenshots kept for reference, not part of the build.
screenshots/
